# Maintainer: Danct12 <danct12@disroot.org>
pkgname=megapixels
pkgver=0.10.0
pkgrel=1
pkgdesc="A camera app that deal with V4L2 request"
url="https://git.sr.ht/~martijnbraam/megapixels"
license=('GPL')
arch=('x86_64' 'armv7h' 'aarch64')
depends=('glib2' 'gtk3')
makedepends=('meson' 'ninja')
replaces=('pinhole' 'gnome-camera')
source=("https://git.sr.ht/~martijnbraam/megapixels/archive/${pkgver}.tar.gz")
sha256sums=('cb4ce9f1a0a06f7cc668e43110d068c61ff3c54f836d3aa875c7ee522f4cfc54')

build() {
    arch-meson $pkgname-$pkgver build
    ninja -C build
}

package() {
    DESTDIR="$pkgdir" ninja -C build install
}
